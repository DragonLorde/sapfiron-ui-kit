import styled from 'styled-components'

const BlockRowContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  width: 100%;
  height: auto;
  gap: 16px;
`

export const Styled = {
  BlockRowContainer,

}
